
general:
  - name: groups
    primary: displayName
    exclude:
      - id
      - lastModifiedDateTime
      - createdDateTime
      - version
      - renewedDateTime

deviceManagement:
  - name: deviceConfigurations
    primary: displayName
    powershell: New-DeviceManagement_DeviceConfigurations
    exclude:
      - id
      - lastModifiedDateTime
      - createdDateTime
      - version

deviceAppManagement:
  - name: androidManagedAppProtections
    primary: displayName
    exclude:
      - id
      - lastModifiedDateTime
      - createdDateTime
      - version

  - name: iosManagedAppProtections
    primary: displayName
    exclude:
      - id
      - lastModifiedDateTime
      - createdDateTime
      - version
