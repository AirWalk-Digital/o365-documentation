deviceManagement:
  - name: deviceConfigurations
    primary: displayName
    powershell: New-DeviceManagement_DeviceConfigurations
    exclude:
      # - '@odata.type'
      - id
      - lastModifiedDateTime
      - createdDateTime
      - version
      - edgeHomepageUrls # Temp
